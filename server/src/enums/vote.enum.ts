export enum VoteTargetType {
  Question = 'Question',
  Answer = 'Answer',
}

export enum VoteValue {
  Upvote = 1,
  Downvote = -1,
}

export enum ReputationPoints {
  QuestionUp = 5,
  QuestionDown = -1,
  AnswerUp = 10,
  AnswerDown = -2,
}
